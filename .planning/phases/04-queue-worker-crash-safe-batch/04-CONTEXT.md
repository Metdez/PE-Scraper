# Phase 4: Queue, Worker & Crash-Safe Batch - Context

Use SQLite atomic claims and per-firm commits. Failed firms must remain visible
without stopping the batch. Export the fixed schema to CSV and styled Excel.
