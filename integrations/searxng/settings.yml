use_default_settings: true

general:
  debug: false
  instance_name: "PE Scraper Search"

search:
  safe_search: 0
  formats:
    - html
    - json

server:
  secret_key: "pe-scraper-local-only-change-before-public-exposure"
  limiter: false
  image_proxy: false
