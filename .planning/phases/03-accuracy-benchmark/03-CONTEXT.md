# Phase 3: Accuracy Benchmark - Context

Measure extraction and page-selection separately using repeatable JSONL fixtures.
Keep the harness offline-capable so prompt/model changes can be regression tested.
