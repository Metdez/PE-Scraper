---
phase: 02-core-pipeline-single-firm
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pescraper/db.py
  - tests/test_db.py
autonomous: true
requirements: [PIPE-05]

must_haves:
  truths:
    - "a firm previously written via upsert_firm can be read back byte-equivalent via get_firm"
    - "every insert_extraction call produces exactly one new row in the extractions table with a non-empty created_at timestamp"
    - "no SQL in this module is ever built by string formatting/concatenation of caller-supplied values"
  artifacts:
    - "src/pescraper/db.py::get_firm"
    - "src/pescraper/db.py::insert_extraction"
  key_links:
    - "cli.py's run_firm() (02-06) calls get_firm() before merging and insert_extraction() once per extracted field"
    - "ingest.py's ingest_csv() (02-05) calls get_firm() before seeding a row"
---

<objective>
Extend Phase 1's `db.py` (the sole SQL-writing module, per the established convention) with the
two read/write helpers Phase 2 needs and Phase 1 didn't yet provide: reading a full `FirmRecord`
back out by website (needed by the universal merge rule), and inserting one per-field provenance
row into the already-existing `extractions` table (needed to satisfy PIPE-05).

Purpose: No new tables or columns — Phase 1's schema already has everything Phase 2's
provenance requirement needs (per CONTEXT.md: "no schema change required, just population").
This plan is the population seam.
Output: `pescraper.db.get_firm()` and `pescraper.db.insert_extraction()`, both parameterized,
both crash-safe (per-call commit), both unit-tested against a `tmp_path` SQLite file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-core-pipeline-single-firm/02-CONTEXT.md
@src/pescraper/db.py
@src/pescraper/models.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: get_firm — read a FirmRecord back by website</name>
  <files>src/pescraper/db.py, tests/test_db.py</files>
  <read_first>
    src/pescraper/db.py (existing `upsert_firm`, `advance_status`, `_FIRM_COLUMN_TYPES`,
    row_factory=sqlite3.Row). src/pescraper/models.py (`FIRM_COLUMNS`, `FirmRecord`,
    `FirmStatus`) — status is stored as TEXT, needs_review as INTEGER 0/1; both must be
    converted back to their Python/enum types on read.
  </read_first>
  <behavior>
    - get_firm(conn, "https://no-such-firm.example") -> None (no row).
    - Upserting a fully-populated FirmRecord, then calling get_firm(conn, its website) returns a FirmRecord equal in every field to the original (round-trip, including status enum and needs_review bool).
    - Upserting a minimal FirmRecord (only firm_name + website set, everything else null-by-default) round-trips correctly too — null criteria fields stay None, not "None" strings or 0.
  </behavior>
  <action>
    Add `get_firm(conn: sqlite3.Connection, website: str) -> FirmRecord | None`. Query
    `SELECT * FROM firms WHERE website = ?` with a parameterized placeholder (never
    string-format `website` into the SQL). If no row, return `None`. Otherwise build a dict
    from the `sqlite3.Row` (row_factory is already `sqlite3.Row` per `connect()`), convert the
    `status` TEXT value to `FirmStatus(row["status"])` and `needs_review` INTEGER to `bool`,
    and construct `FirmRecord(**data)`. Add `"get_firm"` to `__all__`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_db.py -k "get_firm" -x -q</automated>
  </verify>
  <done>
    get_firm returns None for a missing website and a field-equal FirmRecord (including
    correct FirmStatus/bool coercion) for an existing one, verified via upsert-then-read
    round-trip tests against a tmp_path database.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: insert_extraction — one provenance row per extracted field</name>
  <files>src/pescraper/db.py, tests/test_db.py</files>
  <read_first>
    src/pescraper/db.py `_SKELETON_DDL`'s `extractions` table definition (columns: id,
    firm_website, source_page_url, field, value, quote, model, prompt_version, content_hash,
    created_at).
  </read_first>
  <behavior>
    - insert_extraction with all fields populated adds exactly one row to `extractions` whose columns match the call's keyword arguments exactly, and whose `created_at` is a non-empty ISO timestamp.
    - insert_extraction with `source_page_url=None`, `quote=None` (an unmatched/unverified field, per provenance.find_source_page returning None) still inserts a row — the row itself is the record that this field's provenance couldn't be verified, not a reason to skip persisting it.
    - Calling insert_extraction twice for the same firm/field produces two rows (append-only log, not an upsert) — one row per extraction run per field, matching "one row per extracted field per firm run" from CONTEXT.md.
  </behavior>
  <action>
    Add `insert_extraction(conn: sqlite3.Connection, *, firm_website: str, field: str, value:
    str | None, quote: str | None, source_page_url: str | None, model: str, prompt_version:
    str, content_hash: str | None) -> None`. Build `created_at =
    datetime.now(timezone.utc).isoformat()` (import `datetime`/`timezone` from stdlib at module
    top). Execute a parameterized `INSERT INTO extractions (firm_website, source_page_url,
    field, value, quote, model, prompt_version, content_hash, created_at) VALUES (...)` using
    named placeholders (matching `upsert_firm`'s existing parameterization style — never
    f-string/format caller-supplied values into the SQL text). Commit. Add
    `"insert_extraction"` to `__all__`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_db.py -k "insert_extraction" -x -q</automated>
  </verify>
  <done>
    insert_extraction is importable from `pescraper.db`, writes exactly one committed row per
    call with a real timestamp, handles source_page_url/quote=None gracefully, and repeated
    calls append rather than overwrite.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller-supplied field values -> SQL | `get_firm`/`insert_extraction` receive strings ultimately sourced from scraped web content, LLM output, or an untrusted CSV |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering | db.get_firm / db.insert_extraction | high | mitigate | Both functions use parameterized queries exclusively (`?`/`:name` placeholders), continuing `upsert_firm`'s existing Phase 1 pattern — never string-format or concatenate caller-supplied values into SQL text. Verified by test cases using deliberately SQL-metacharacter-laden field/value strings (e.g. containing `'`, `;`, `--`) that must round-trip as plain data, not execute |
</threat_model>

<verification>
`uv run pytest tests/test_db.py -q` (full file, including Phase 1's existing lifecycle/staleness
tests, to confirm no regression).
</verification>

<success_criteria>
db.py exposes get_firm and insert_extraction, both parameterized and unit-tested, ready for
02-05 (ingest) and 02-06 (integration) to build on.
</success_criteria>

<output>
Create `.planning/phases/02-core-pipeline-single-firm/02-02-SUMMARY.md` when done
</output>
